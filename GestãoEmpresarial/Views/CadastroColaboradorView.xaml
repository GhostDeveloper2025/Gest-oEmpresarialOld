<UserControl x:Class="GestãoEmpresarial.Views.CadastroColaboradorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestãoEmpresarial.Views"
             xmlns:utils="clr-namespace:GestãoEmpresarial.Utils"
             xmlns:cc="clr-namespace:GestãoEmpresarial.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UniformGrid>
        <cc:CelularTextBox
            Text="{Binding ObjectoEditar.Telefone, 
            UpdateSourceTrigger=PropertyChanged}"/>
        <cc:MoedaTextBox
                         Text="{Binding ObjectoEditar.Comissao, 
            Converter={utils:DecimalConverter}}"/>
        <TextBox
            Text="{Binding ObjectoEditar.Senha, 
            UpdateSourceTrigger=PropertyChanged}"/>
    </UniformGrid>
</UserControl>
