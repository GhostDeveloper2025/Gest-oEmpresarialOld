<Window x:Class="GestãoEmpresarial.Views.Cadastro.DialogImprimir"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:GestãoEmpresarial.Views.Cadastro"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="CadastroSucessoImprimir"
        Width="500"
        Height="200"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        mc:Ignorable="d">
    <Border Background="{StaticResource SecondaryColor0}"
            BorderBrush="{StaticResource SecondaryColor5}"
            BorderThickness="0"
            CornerRadius="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <!--  Barra de Título Customizada  -->
            <Border Grid.Row="0"
                    Height="30"
                    VerticalAlignment="Top"
                    Background="{StaticResource SecondaryColor0}"
                    CornerRadius="15,15,0,0">
                <Grid>
                    <TextBlock Margin="10,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource FontColor1}"
                               Text="Cadastro Sucesso" />
                    <Button Width="30"
                            Height="30"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Background="{StaticResource PrimaryColor6}"
                            BorderBrush="{StaticResource PrimaryColor2}"
                            Click="FecharJanela_Click"
                            Content="X"
                            FontWeight="Bold"
                            Foreground="Black" />

                    <!--<Button Command="{Binding SaveCommand}" HorizontalAlignment="Right" Style="{StaticResource ButonFecharStyle}" Click="FecharJanela_Click" Margin="0,10,15,10"/>-->

                </Grid>
            </Border>
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           Foreground="{StaticResource FontColor1}"
                           Text="Quer Imprimir?" />
            </StackPanel>
            <!--  Conteúdo da Janela  -->
            <StackPanel Grid.Row="2"
                        Margin="10"
                        VerticalAlignment="Bottom">
                <local:BotaoImprimir HorizontalAlignment="Left" FecharJanela="True" />
            </StackPanel>
        </Grid>
    </Border>
</Window>