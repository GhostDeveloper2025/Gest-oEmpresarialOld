<TextBox x:Class="GestãoEmpresarial.CustomControls.ValidarTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestãoEmpresarial.CustomControls"
             Style="{StaticResource StyleTB}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Validation.ErrorTemplate>
        <ControlTemplate>
            <StackPanel>
                <!-- Placeholder for the TextBox itself -->
                <AdornedElementPlaceholder x:Name="textBox"/>
                <TextBlock Margin="10" Text="{Binding [0].ErrorContent}" Foreground="Red"/>
                <AdornedElementPlaceholder x:Name="textBoxBelow"/>
            </StackPanel>
        </ControlTemplate>
    </Validation.ErrorTemplate>
</TextBox>
